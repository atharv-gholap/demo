name:  TASK-1 first workflow

on:  
   workflow_dispatch:
   # push:  
   #  branches: 
   #    - main

jobs:
  hello-job:
    runs-on:  ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello DevOps world"
   
